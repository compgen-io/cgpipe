# 07-custom-directives: job.custom entries appear verbatim, each prefixed by
# `#SBATCH ` for SLURM.

output.txt:
    <%
    job.custom = ["--gres=gpu:1", "--exclusive"]
    %>
    echo hi > output.txt
