# 23-container-gpu-docker-count: integer >1 maps directly to `--gpus N`.

cgpipe.container.engine = "docker"
cgpipe.container.body_dir = "/cgpipe-bodydir"
job.container = "nvidia/cuda:12.0-base"

output.txt:
    <%
        job.gpu = 2
    %>
    multi-gpu-program > output.txt
