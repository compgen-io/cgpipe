# 08-setup-block: job.setup lines run before the body inside the submitted
# script (e.g. `module load ...` is the usual idiom).

output.txt:
    <%
    job.setup = ["module load foo", "module load bar"]
    %>
    echo hi > output.txt
